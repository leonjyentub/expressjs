doctype html
html
    head
        // Required meta tags
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
        // Bootstrap CSS
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" 
                            integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" 
                            crossorigin="anonymous")
        title= "測試頁"
    body
        - var i = 10
        p= i
        - i = i + 1
        p i 這個變數的值目前是#{i}
        hr
        - var height = 171.5, weight = 100;
        - var bmi = weight / ((height/100) * (height/100));
        p= "result=" + bmi
        div#user
            if bmi < 18.5
                h2(style="color:blue")= "體重過輕"
            else if bmi >= 18.5 && bmi < 24
                h2(style="color:green")= "正常範圍"
            else
                h2(style="color:red")= "太胖了"
        hr
        - var i = 1
        ul
            while i <= 10
                li 第#{i}個項目
                - i++

        ul
            each val in [1, 2, 3, 4, 5]
                li= "第" + val + "個"

        select#test
            each val, index in ["我是左邊", "這是中間", "最後一個", "只是想多個東西"]
                option(value=index) 選項：#{val}

        p
        
        each val, key in {5: "五專", 2:"二技", 4:"四技"}
            input(id="rd-" + key, type="radio", name="schsystem", value=key)
            label(for="rd-" + key)= val
            