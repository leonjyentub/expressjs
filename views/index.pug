extends layout

block cusCss
  style.
    .footer {
      position: absolute;
      bottom: 0;
      width: 100%;
      height: 60px; /* Set the fixed height of the footer here */
      line-height: 60px; /* Vertically center the text there */
      background-color: #f5f5f5;
    }

block content
  h1= title
  p Welcome to #{title}

block foot
  .footer
    .container
      span.text-muted 我是網頁最下面
